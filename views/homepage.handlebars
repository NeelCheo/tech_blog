<h1>Welcome to Tech Blog</h1>

{{#each posts as |post|}}
  <div class="post">
    <a href="/post/{{post.id}}">
      <h2>{{post.title}}</h2>
      <p>Created by {{post.user.username}} on {{format_date post.createdAt}}</p>
      <p>{{post.body}}</p>
    </a>
  </div>
{{/each}}
