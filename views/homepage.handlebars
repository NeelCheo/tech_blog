<h1>Welcome to Tech Blog</h1>

{{#each posts as |post|}}
  <div class="post">
    <h2>{{post.title}}</h2>
    <p>Created by {{post.username}} on {{post.created_at}}</p>
    <p>{{post.body}}</p>
  </div>
{{/each}}

