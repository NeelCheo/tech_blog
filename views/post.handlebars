<h1>{{post.title}}</h1>
<p>Posted by {{post.username}} on {{post.created_at}}</p>
<p>{{post.body}}</p>

<h2>Comments</h2>
{{#each post.comments as |comment|}}
  <div class="comment">
    <p>{{comment.body}}</p>
    <p>Posted by {{comment.username}} on {{comment.created_at}}</p>
  </div>
{{/each}}
